package project.be_sep490_g67.enums;

/** Phạm vi kỳ dùng khi xuất sổ doanh thu S1a-HKD dạng xem trước. */
public enum S1aPeriodType {
    /** Một tab cho một tháng được chọn. */
    MONTH,
    /** Ba tab tương ứng với ba tháng trong quý được chọn. */
    QUARTER,
    /** Một tab cho mỗi tháng trong năm. */
    YEAR
}
