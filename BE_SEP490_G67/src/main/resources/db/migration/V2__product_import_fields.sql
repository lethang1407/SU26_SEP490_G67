-- Placeholder: product import columns already applied via Hibernate ddl-auto=update
-- (products.status, season_tag, cover_days_override; categories.cover_days,
--  default_supplier_id; store_config.default_cover_days; suppliers.lead_time_days).
-- Keep this version so Flyway history stays contiguous if migrations are re-enabled.
SELECT 1;
